# 📄 Форматы Файлов KiOS

**Версия:** 1.0  
**Статус:** Спецификация

---

## 🎯 Обзор

KiOS использует собственные бинарные форматы файлов, оптимизированные под архитектуру и потребности системы.

---

## 🟢 KiELF — Исполняемый Файл

**Расширение:** `.kelf`  
**Magic:** `0x4B454C46` ("KELF")

### Структура Заголовка (64 байта)

```c
struct KelfHeader {
    uint32_t magic;           // 0x4B454C46
    uint16_t version;         // Версия формата (1)
    uint16_t type;            // 1=EXE, 2=SO, 3=OBJ
    uint32_t entry;           // Точка входа (адрес)
    uint32_t phoff;           // Program headers offset
    uint32_t shoff;           // Section headers offset
    uint32_t flags;           // Флаги
    uint16_t phentsize;       // Размер program header
    uint16_t phnum;           // Количество program headers
    uint16_t shentsize;       // Размер section header
    uint16_t shnum;           // Количество section headers
    uint16_t shstrndx;        // Индекс строковой таблицы
    uint8_t  abi;             // ABI (0 = KiOS)
    uint8_t  arch;            // Архитектура (0x40 = x86_64)
    uint16_t reserved;       // Зарезервировано
};
```

### Program Headers

```c
struct KelfProgramHeader {
    uint32_t type;           // PT_LOAD, PT_DYNAMIC, PT_INTERP
    uint32_t flags;           // PF_X, PF_W, PF_R
    uint64_t offset;         // Смещение в файле
    uint64_t vaddr;          // Виртуальный адрес
    uint64_t paddr;          // Физический адрес
    uint64_t filesz;         // Размер в файле
    uint64_t memsz;          // Размер в памяти
    uint64_t align;          // Выравнивание
};
```

### Типы Секций

| Тип | Описание |
|-----|----------|
| `.text` | Исполняемый код |
| `.data` | Инициализированные данные |
| `.rodata` | Константы (только чтение) |
| `.bss` | Неинициализированные данные |
| `.symtab` | Таблица символов |
| `.strtab` | Строки символов |
| `.rel` | Релокации |
| `.export` | Экспортируемые функции |
| `.import` | Импортируемые функции |

---

## 🟢 KiFS — Файловая Система

**Расширение:** `.kfs`  
**Magic:** `0x4B465332` ("KFS2")

### Структура Суперблока (1 KB)

```c
struct KfsSuperblock {
    uint32_t magic;           // 0x4B465332
    uint16_t version;         // Версия (1)
    uint16_t blocksize;      // Размер блока (512, 1024, 2048, 4096)
    uint32_t block_count;     // Всего блоков
    uint32_t inode_count;    // Количество inode
    uint32_t free_blocks;    // Свободных блоков
    uint32_t free_inodes;    // Свободных inode
    uint32_t first_data;     // Первый блок данных
    uint32_t inode_table;     // Блок начала таблицы inode
    uint64_t created;       // Время создания (Unix timestamp)
    uint64_t modified;       // Время модификации
    uint32_t root_inode;     // Inode корневой директории
    uint8_t  uuid[16];       // UUID файловой системы
    char     label[64];      // Метка тома
    uint8_t  reserved[???]; // Заполнение до 1024 байт
};
```

### Inode (256 байт)

```c
struct KfsInode {
    uint16_t mode;            // Тип и права (rwxrwxrwx)
    uint16_t uid;             // Owner UID
    uint32_t size;            // Размер файла (bytes)
    uint32_t atime;          // Last access
    uint32_t mtime;          // Last modified
    uint32_t ctime;          // Last status change
    uint32_t dtime;          // Deletion time
    uint32_t gid;            // Group ID
    uint32_t links;          // Количество ссылок
    uint64_t blocks;         // Количество блоков
    uint32_t flags;          // Флаги файла
    uint32_t direct[12];     // Прямые указатели на блоки
    uint32_t indirect;       // Одиночно косвенный
    uint32_t double_indirect; // Дважды косвенный
    uint32_t triple_indirect; // Трижды косвенный
    uint32_t reserved[???];  // Заполнение до 256 байт
};
```

### Режимы Файлов

| Режим | Тип |
|-------|-----|
| `0x1000` | Сокет |
| `0x2000` | Символьное устройство |
| `0x4000` | Директория |
| `0x6000` | Блочное устройство |
| `0x8000` | Обычный файл |
| `0xA000` | Ссылка |

### Права Доступа

| Бит | Право |
|-----|-------|
| `0400` | Владелец: чтение |
| `0200` | Владелец: запись |
| `0100` | Владелец: выполнение |
| `0040` | Группа: чтение |
| `0020` | Группа: запись |
| `0010` | Группа: выполнение |
| `0004` | Остальные: чтение |
| `0002` | Остальные: запись |
| `0001` | Остальные: выполнение |

---

## 🟢 KiTX — Текстура

**Расширение:** `.ktx`  
**Magic:** `0x4B5458` ("KTX")

### Заголовок (64 байта)

```c
struct KtxHeader {
    uint32_t magic;           // 0x4B5458
    uint16_t version;         // Версия (1)
    uint16_t width;          // Ширина (px)
    uint16_t height;         // Высота (px)
    uint8_t  bpp;             // Bits per pixel (8, 16, 24, 32)
    uint8_t  channels;       // Каналы (1=灰, 2=GA, 3=RGB, 4=RGBA)
    uint8_t  format;         // Формат данных
    uint8_t  compression;    // Сжатие (0=none, 1=RLE, 2=LZ4)
    uint32_t mipmap_count;   // Количество mipmaps
    uint32_t data_size;      // Размер данных
    uint8_t  has_alpha;      // Есть альфа
    uint8_t  premultiplied;  // Премультиплицированный альфа
    uint16_t reserved;       // Зарезервировано
};
```

### Форматы Пикселей

| Код | Формат |
|-----|--------|
| 0 | Grayscale (8-bit) |
| 1 | RGB (5-6-5) |
| 2 | RGB (8-8-8) |
| 3 | RGBA (4-4-4-4) |
| 4 | RGBA (8-8-8-8) |
| 5 | BGRA (8-8-8-8) |

---

## 🟢 KiIMG — Изображение

**Расширение:** `.kimg`  
**Magic:** `0x4B494D47` ("KIMG")

### Заголовок (128 байт)

```c
struct KimgHeader {
    uint32_t magic;           // 0x4B494D47
    uint16_t version;         // Версия (1)
    uint16_t type;            // 1=Bitmap, 2=Vector, 3=Layered
    
    uint32_t width;           // Ширина
    uint32_t height;         // Высота
    uint8_t  color_mode;     // Цветовая модель
    
    uint32_t layer_count;    // Количество слоёв
    uint32_t metadata_size;  // Размер метаданных
    
    // Для анимации
    uint16_t fps;            // Кадров в секунду
    uint16_t frame_count;    // Количество кадров
    
    uint64_t created;        // Время создания
    uint64_t modified;       // Время модификации
    
    char author[64];         // Автор
    char description[256];  // Описание
    
    uint32_t data_offset;    // Смещение к данным
    uint32_t reserved[???];  // Заполнение
};
```

### Типы Изображений

| Тип | Описание |
|-----|----------|
| 1 | Bitmap — простое растровое изображение |
| 2 | Vector — векторное (SVG-подобный) |
| 3 | Layered — многослойное (для редакторов) |

---

## 🟢 KiFONT — Шрифт

**Расширение:** `.kfont`  
**Magic:** `0x4B464E54` ("KFNT")

### Заголовок (64 байта)

```c
struct KfontHeader {
    uint32_t magic;           // 0x4B464E54
    uint16_t version;         // Версия
    uint8_t  font_type;       // 0=Bitmap, 1=Vector
    uint8_t  style;           // Regular, Bold, Italic, etc.
    
    uint16_t units_per_em;   // Единиц на em
    uint16_t ascender;       // Ascender
    uint16_t descender;      // Descender
    uint16_t line_gap;       // Межстрочный интервал
    
    uint16_t num_glyphs;     // Количество глифов
    uint16_t num_tables;     // Количество таблиц
    
    uint32_t glyph_offset;   // Смещение к глифам
    uint32_t tables_offset;  // Смещение к таблицам
    
    char family[32];         // Название семейства
    char weight[16];         // Название веса
    
    // Для bitmap шрифтов
    uint8_t  pixel_size;     // Размер пикселей
    uint8_t  bit_depth;      // Глубина
    uint8_t  reserved[2];
};
```

---

## 🟢 KiPKG — Пакет

**Расширение:** `.kpkg`  
**Magic:** `0x4B504B47` ("KPKG")

### Заголовок

```c
struct KpkgHeader {
    uint32_t magic;           // 0x4B504B47
    uint16_t version;        // Версия пакета
    uint16_t type;           // 1=App, 2=Driver, 3=System
    
    char name[64];           // Имя пакета
    char version[16];        // Версия (semver)
    char author[32];         // Автор
    
    uint64_t installed_size; // Установленный размер
    uint64_t archive_size;   // Размер архива
    
    uint32_t entry_count;    // Количество файлов
    uint32_t depends_count;  // Количество зависимостей
    
    uint32_t scripts_offset; // Скрипты установки
    uint32_t files_offset;   // Смещение к файлам
    
    uint8_t  compression;   // 0=Store, 1=LZ4, 2=ZSTD
    uint8_t  flags;          // Флаги
    uint16_t reserved;
};
```

---

## 🟢 KiCFG — Конфигурация

**Расширение:** `.kcfg`  
**Текстовый/Бинарный формат**

### Пример (Текстовый)

```ini
[System]
version=1.0
language=ru-RU
timezone=Europe/Minsk

[Display]
width=1024
height=768
bpp=32
vsync=true

[Boot]
default_os=KiOS
timeout=5
```

### Бинарный формат

```c
struct KcfgHeader {
    uint32_t magic;           // 0x4B434647
    uint16_t version;
    uint16_t entry_count;
    uint32_t data_size;
};
```

---

## 🟢 KiSH — Скрипт

**Расширение:** `.ksh`  
**Интерпретируемый/Компилируемый**

### Заголовок

```c
struct KshHeader {
    uint32_t magic;           // 0x4B5348
    uint16_t version;         // Версия
    uint8_t  type;            // 1=Source, 2=Bytecode
    uint8_t  flags;
    uint32_t bytecode_size;   // Размер байт-кода (если есть)
    uint32_t string_count;    // Строковые константы
};
```

---

## 📋 Сравнение с Windows XP

| KiOS | Windows XP |
|------|------------|
| `.kelf` | `.exe`, `.dll` |
| `.kfs` | NTFS |
| `.ktx` | `.bmp`, `.png` |
| `.kimg` | `.jpg`, `.bmp` |
| `.kfont` | `.ttf`, `.fon` |
| `.kpkg` | `.msi`, `.cab` |
| `.kcfg` | `.ini`, `.reg` |
| `.ksh` | `.bat`, `.cmd` |

---

*Форматы могут изменяться по мере развития системы.*
